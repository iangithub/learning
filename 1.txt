// (1) ===== Create MLContext ==========
            MLContext mlContent = new MLContext();

            // (2) ===== Load trained model ==========
            DataViewSchema modelSchema; //Define DataViewSchema for data preparation pipeline and trained model
            ITransformer trainedModel = mlContent.Model.Load("model.zip", out modelSchema);

            // (3) ===== Single Item ==========
            UseModelWithSingleItem(mlContent, trainedModel);
