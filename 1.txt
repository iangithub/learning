LoadImagesFromDisk();
            var prediction = new CustomVisionPredictionClient()
            {
                ApiKey = _apiKey,
                Endpoint = _endPoint
            };

            var result = prediction.ClassifyImage(_projectId, _modelname, _testImage);

            foreach (var c in result.Predictions)
            {
                Console.WriteLine($"\t{c.TagName}: {c.Probability:P1}");
            }
